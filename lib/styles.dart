import 'package:flutter/material.dart';

const TextStyle bigText = TextStyle(
  fontSize: 24
);

const pagePadding = EdgeInsets.all(12.0);

const defaultButtonColor = Colors.blueGrey;
const primaryButtonColor = Colors.indigo;
